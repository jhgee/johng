box: debian
build:
  steps:
    - script:
        name: install git
        code: |
          apt-get update && apt-get install git -y
    - script:
        name: initialize git submodules
        code: |
            git submodule update --init --recursive
    - arjen/hugo-build:
        version: "0.15"
deploy:
   steps:
    - install-packages:
        packages: lftp
    - script:
        name: ftp deploy - sync with remote
        code: |
          echo $WERCKER_ROOT
          lftp sftp://$USER:$PASS@johng.de/  -e "mirror  --reverse -e -R ./public / --verbose; bye"
    # - duleorlovic/ftp-deploy:
    #     basedir: public/
    #     destination: ftp://johng.de/
    #     username: $FTP-USER
    #     password: $PASS
